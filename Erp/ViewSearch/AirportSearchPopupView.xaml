<UserControl x:Class="Erp.ViewSearch.AirportSearchPopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Erp.ViewSearch"
              xmlns:viewModel="clr-namespace:Erp.ViewModelSearch.Thesis"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf" xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModel:AirportSearchViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>


        <TextBlock Text="Country Code : " FontWeight="Bold"   Grid.Row="1" Grid.Column="0" Margin="2,2,2,2" />
        <StackPanel Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center"  Orientation="Horizontal" Margin="0,2,0,2" Width="135" >
            <TextBox Text="{Binding FilterData.City.CountryCode}" HorizontalAlignment="left" Width="115"   />
            <Button Content="f" Width="20"                    Command="{Binding ShowCountriesGridCommand}"/>
        </StackPanel>

        <TextBlock Text="Prefecture Code : " FontWeight="Bold"   Grid.Row="2" Grid.Column="0" Margin="2,2,2,2" />
        <StackPanel Grid.Row="2" Grid.Column="1" HorizontalAlignment="Center"  Orientation="Horizontal" Margin="0,2,0,2" Width="135" >
            <TextBox Text="{Binding FilterData.City.PrefCode}" HorizontalAlignment="left" Width="115"   />
            <Button Content="f" Width="20"                    Command="{Binding ShowPrefecturesGridCommand}"/>
        </StackPanel>
        <TextBlock Text="City Code : " FontWeight="Bold"   Grid.Row="3" Grid.Column="0" Margin="2,2,2,2" />
        <StackPanel Grid.Row="3" Grid.Column="1" HorizontalAlignment="Center"  Orientation="Horizontal" Margin="0,2,0,2" Width="135" >
            <TextBox Text="{Binding FilterData.City.CityCode}" HorizontalAlignment="left" Width="115"   />
            <Button Content="f" Width="20"                    Command="{Binding ShowCitiesGridCommand}"/>
        </StackPanel>

        <StackPanel  HorizontalAlignment="Center" Orientation="Horizontal" Width="208" Grid.Row="4" Grid.Column="0">
            <Button x:Name="ClearButton"  Width="100" Margin="2" HorizontalAlignment="Left" Command="{Binding ClearCommand}">
                <TextBlock Text="Clear"/>
            </Button>

            <Button x:Name="SearchButton"  Width="100" Margin="2" HorizontalAlignment="Left" Command="{Binding SearchCommand}">
                <TextBlock Text="Search"/>
            </Button>


        </StackPanel>

    </Grid>
</UserControl>
