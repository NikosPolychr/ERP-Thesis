<UserControl x:Class="Erp.View.Thesis.MinMaxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Erp.View.Thesis"
             xmlns:converters="clr-namespace:Erp.Converters" 
             xmlns:viewmodel="clr-namespace:Erp.ViewModel.Thesis"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf" 
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"  FontSize="18">
    <UserControl.DataContext>
        <viewmodel:MinMaxViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>


        <TextBlock Text="MinMax Code : " FontWeight="Bold"   Grid.Row="1" Grid.Column="0" Margin="2" />
        <StackPanel Grid.Row="1" Grid.Column="1" HorizontalAlignment="Left"  Orientation="Horizontal" Margin="2" >
            <TextBox Text="{Binding FlatData.Code}" HorizontalAlignment="left" Width="115"   />
            <Button Content="f" Width="20"  Command="{Binding ShowMinMaxGridCommand}"/>
        </StackPanel>

        <TextBlock Text="MinMax Descr : " FontWeight="Bold"  Grid.Row="1" Grid.Column="2" Margin="2" />
        <StackPanel Grid.Row="1" Grid.Column="3" HorizontalAlignment="Stretch"  Orientation="Horizontal" Margin="2" >
            <TextBox Text="{Binding FlatData.Descr}" HorizontalAlignment="Stretch" Width="135" />
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="4"  HorizontalAlignment="Left" Orientation="Horizontal" Margin="1" >
            <Button x:Name="AddMinMax"  Width="150" Margin = "15,0,0,0"  HorizontalAlignment="Left" Command="{Binding AddMinMaxDataCommand}">
                <TextBlock Text="Add New MinMax"/>
            </Button>

            <ToggleButton x:Name = "tb" Content = "Show Deleted"  
         IsChecked="{Binding ShowDeleted, Mode=TwoWay}"   Background="Beige" BorderBrush="Black"  Margin = "15,0,0,0"
         Width = "120" HorizontalAlignment = "Center" >
            </ToggleButton>
            <TextBlock Text="Delete : " FontWeight="Bold"    Margin="5,0,0,0"/>
            <CheckBox IsChecked="{Binding FlatData.IsDeleted, Mode=TwoWay}"  Margin="1" VerticalAlignment="Center"  />

        </StackPanel>
        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="8"  converters:MainStackPanelReadonly.IsEnabledBasedOnProperty="{Binding FlatData.Code}" >
            <TabControl >
                <TabItem Header="General Information">
                    <StackPanel>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>

                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>

                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Is Act : "    Grid.Row="0" Grid.Column="0" Margin="2" />
                            <TextBlock Text="Is Min : "    Grid.Row="1" Grid.Column="0" Margin="2" />
                            <TextBlock Text="Position : "    Grid.Row="2" Grid.Column="0" Margin="2" />
                            <TextBlock Text="Route Category : "    Grid.Row="3" Grid.Column="0" Margin="2" />
                            <TextBlock Text="Crew Category : "    Grid.Row="4" Grid.Column="0" Margin="2" />

                            <TextBlock Text="Rhs : "    Grid.Row="5" Grid.Column="0" Margin="0" />
                            <TextBlock Text="Penalty Points : "    Grid.Row="6" Grid.Column="0" Margin="2" />

                            <CheckBox IsChecked="{Binding FlatData.IsAct, Mode=TwoWay}" Grid.Row="0" Grid.Column="1" Margin="2" HorizontalAlignment="Stretch" />
                            <CheckBox IsChecked="{Binding FlatData.IsMin, Mode=TwoWay}" Grid.Row="1" Grid.Column="1" Margin="2" HorizontalAlignment="Stretch" />

                            <ComboBox ItemsSource="{Binding Positions}"  SelectedItem="{Binding FlatData.Position , UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                                      Grid.Row="2" Grid.Column="1" Margin="2"/>
                            <ComboBox ItemsSource="{Binding RouteCategories}"  SelectedItem="{Binding FlatData.RouteCateg , UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                      Grid.Row="3" Grid.Column="1" Margin="2"/>
                            
                            <StackPanel Grid.Row="4" Grid.Column="1" HorizontalAlignment="Left"  Orientation="Horizontal" Margin="2" >
                                <TextBox Text="{Binding FlatData.CrewCat.CrewCatCode , Mode=TwoWay}" HorizontalAlignment="left" Width="100"   />
                                <Button Content="f" Width="20"                    Command="{Binding ShowCrewCategGridCommand}"/>
                            </StackPanel>


                            <TextBox Text="{Binding FlatData.Rhs, Mode=TwoWay}" Grid.Row="5" Grid.Column="1" Margin="2" HorizontalAlignment="Stretch" Width="120" />
                            <TextBox Text="{Binding FlatData.PenaltyPoints, Mode=TwoWay}" Grid.Row="6" Grid.Column="1" Margin="2" HorizontalAlignment="Stretch" Width="120" />


                        </Grid>
                        <StackPanel  HorizontalAlignment="Left" Orientation="Horizontal">
                            <Button x:Name="ClearButton"  Width="100" Margin="2" HorizontalAlignment="Left" Command="{Binding ClearCommand}">
                                <TextBlock Text="Clear"/>
                            </Button>

                            <Button x:Name="RefreshButton"  Width="100" Margin="2" HorizontalAlignment="Left" Command="{Binding RefreshCommand}">
                                <TextBlock Text="Refresh"/>
                            </Button>

                            <Button x:Name="SaveButton"  Width="100" Margin="2" HorizontalAlignment="Left"  Command="{Binding SaveCommand}">
                                <TextBlock Text="Save"/>
                            </Button>

                        </StackPanel>

                    </StackPanel>
                </TabItem>
            </TabControl>
        </StackPanel>

    </Grid>
</UserControl>
